enum Role {
    ADMIN
    BUSINESS
    CUSTOMER
}

enum TokenType {
    ACCESS
    REFRESH
}

enum OTPType {
    RESET_PASSWORD_OTP
    RESET_EMAIL_OTP
    VERIFY_OTP
}

enum OrderStatus {
    PENDING
    CONFIRMED
    PREPARING
    DELIVERING
    DELIVERED
    CANCELLED
}

enum PaymentMethod {
    ZALOPAY
    MOMO
    BANK
    CASH
}

enum PaymentStatus {
    FAILED
    SOLVING
    DONE
}

